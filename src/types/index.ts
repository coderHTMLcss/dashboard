export type TableProps = {
  id: number;
  client: string;
  city: string;
  email: string;
  project: string;
  progress: string;
  status: string;
  date: string;
  image: string;
};

export type Inputs = {
  label: string;
  type: string;
  name: string;
};
